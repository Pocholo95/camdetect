services:
  face-presence:
    build: .
    container_name: face-presence
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
